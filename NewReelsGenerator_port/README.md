# 🎬 Reels Generator

Веб-сервис и мобильное приложение для автоматизации генерации видео и каруселей для Instagram.

## 🚀 Возможности

### 📹 Генерация видео (Reels)
- Создание Reels с наложением текста и музыки
- Выбор области для вставки заголовка
- Настройка шрифтов и цветов
- Автогенерация с настройкой количества
- Интеграция с соцсетями

### 🎠 Генерация каруселей
- **Режим 1**: Готовые фоны + пользовательские изображения
- **Режим 2**: Пользовательские изображения с текстом
- **Режим 3**: AI-генерация изображений по промптам
- **Режим 4**: Style-from-Photo — 1 исходное фото + N-1 AI-изображений «в стиле» ✨ NEW
- Настройка шаблонов и автовыкладка

### 🤖 AI-интеграция
- Автоматическая генерация текстов
- AI-генерация изображений
- Умный подбор музыки

### 👥 Система пользователей
- Регистрация и авторизация
- Лимиты генерации
- Подключение соцсетей
- Админ-панель

## 🛠 Технологии

### Backend
- **FastAPI** - веб-фреймворк
- **SQLAlchemy** - ORM
- **SQLite** - база данных
- **JWT** - аутентификация
- **Celery** - очереди задач

### Frontend
- **React** + **TypeScript**
- **Ant Design** - UI компоненты
- **React Query** - управление состоянием
- **React Router** - маршрутизация

### Mobile
- **React Native** + **Expo**
- **React Navigation** - навигация
- **Axios** - HTTP клиент

## 📦 Установка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd NewReelsGenerator_port
```

### 2. Установка зависимостей Python
```bash
pip install -r requirements.txt
```

### 3. Установка зависимостей Frontend
```bash
cd frontend
npm install
```

### 4. Установка зависимостей Mobile
```bash
cd mobile
npm install
```

## 🚀 Запуск

### Веб-сервер
```bash
python run_server.py
```

### Frontend (в отдельном терминале)
```bash
cd frontend
npm start
```

### Mobile (в отдельном терминале)
```bash
cd mobile
npm start
```

## 📱 Использование

### Веб-интерфейс
1. Откройте http://localhost:3000
2. Зарегистрируйтесь или войдите
3. Выберите тип генерации (видео/карусели)
4. Настройте параметры и запустите генерацию

### Мобильное приложение
1. Установите Expo Go на телефон
2. Запустите `npm start` в папке mobile
3. Отсканируйте QR-код
4. Используйте приложение

### API
- Документация: http://localhost:8000/docs
- Админ-панель: http://localhost:8000/api/admin

## 🔧 Конфигурация

### Переменные окружения
Создайте файл `.env`:
```env
SECRET_KEY=your-secret-key
OPENAI_API_KEY=your-openai-key
INSTAGRAM_API_KEY=your-instagram-key
TIKTOK_API_KEY=your-tiktok-key

# AI для генерации изображений
AITUNNEL_API_KEY=your-aitunnel-api-key
AITUNNEL_MODEL=gemini-2.5-flash-image
AITUNNEL_MODEL_BG=flux-fast

# Режим генерации фонов (дешевый)
USE_LOCAL_BG_GENERATION=true
```

### Настройка лимитов
В админ-панели можно настроить:
- Дневные лимиты генерации
- Лимиты файлов
- Настройки пользователей

## 📊 Структура проекта

```
NewReelsGenerator_port/
├── app/                    # Backend приложение
│   ├── api/               # API роуты
│   ├── core/              # Основные модули
│   ├── models/            # Модели БД
│   └── services/          # Бизнес-логика
├── frontend/              # React веб-приложение
├── mobile/                # React Native приложение
├── static/                # Статические файлы
├── uploads/               # Загруженные файлы
├── output/                # Сгенерированные файлы
└── requirements.txt    # Python зависимости
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для фичи
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

MIT License

## 🆘 Поддержка

Если у вас есть вопросы или проблемы:
1. Проверьте документацию API
2. Посмотрите логи в папке `logs/`
3. Создайте Issue в репозитории

---

## 🎨 Использование режима Style-from-Photo

### Описание

Режим **Style-from-Photo** позволяет создать карусель из N слайдов, где:
- **Слайд 1**: всегда исходное загруженное изображение
- **Слайды 2..N**: AI-сгенерированные изображения в стиле исходного

### API Пример

#### 1. Загрузите референсное изображение

```bash
curl -X POST "http://localhost:8000/api/carousel/upload-style-image" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "file=@your_photo.jpg"
```

Ответ:
```json
{
  "filename": "your_photo.jpg",
  "path": "uploads/style_from_photo/abc123_your_photo.jpg",
  "size": 1024000
}
```

#### 2. Сгенерируйте тексты для карусели

```bash
curl -X POST "http://localhost:8000/api/carousel/generate-texts" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "idea": "5 ошибок в продажах, которые стоят вам денег",
    "slides_count": 5
  }'
```

#### 3. Запустите генерацию карусели

```bash
curl -X POST "http://localhost:8000/api/carousel/generate" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "mode": "style_from_photo",
    "style_image_path": "uploads/style_from_photo/abc123_your_photo.jpg",
    "slides_count": 5,
    "prompt_hint": "dark futuristic atmosphere",
    "style_strength": 0.7,
    "text_content": [
      {"title": "Обложка", "items": []},
      {"title": "Ошибка 1", "items": ["Текст пункта 1"]},
      {"title": "Ошибка 2", "items": ["Текст пункта 2"]},
      {"title": "Ошибка 3", "items": ["Текст пункта 3"]},
      {"title": "Итог", "items": ["Заключение"]}
    ]
  }'
```

#### 4. Проверьте статус генерации

```bash
curl "http://localhost:8000/api/carousel/generations/{job_id}" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Параметры

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `mode` | string | ✅ | `"style_from_photo"` |
| `style_image_path` | string | ✅ | Путь к загруженному референсному изображению |
| `slides_count` | int | ✅ | Общее количество слайдов (1 оригинал + N-1 сгенерированных) |
| `prompt_hint` | string | ❌ | Подсказка для тематики изображений |
| `style_strength` | float | ❌ | Сила переноса стиля (0.0-1.0, по умолчанию 0.6) |
| `seed` | int | ❌ | Seed для воспроизводимости |
| `text_content` | array | ✅ | Тексты для наложения на каждый слайд |

### Ограничения

- **Формат файла**: JPEG, PNG
- **Максимальный размер**: 10 МБ
- **Количество слайдов**: 2-20
- **Resolution**: 1080x1350 (Instagram carousel)

### Настройка провайдера AI

#### AITunnel (основной провайдер)

В `.env` добавьте:
```env
AITUNNEL_API_KEY=your-api-key
AITUNNEL_BASE_URL=https://api.aitunnel.ru/v1
AITUNNEL_MODEL=gemini-2.5-flash-image        # Основная модель (дефолт)
AITUNNEL_MODEL_FALLBACK=gpt-5-image           # Запасная модель (дефолт)
AITUNNEL_MODEL_CANDIDATES=gemini-2.5-flash-image,gpt-5-image  # CSV список кандидатов
AITUNNEL_SEND_MODE=auto                       # auto|multipart|json
AITUNNEL_TIMEOUT_SEC=120                      # Таймаут запросов
```

**Режимы отправки (автоопределение по модели):**
- `gemini-*` → всегда JSON (base64)
- `gpt-5-image` → AUTO (multipart → json fallback)
- остальные → значение из `AITUNNEL_SEND_MODE`

**Автодетект рабочей модели:**
Система автоматически определяет доступную модель через быстрый ping-тест (64x64) при первом запросе и кэширует результат. Порядок проверки: `AITUNNEL_MODEL` → `AITUNNEL_MODEL_FALLBACK` → `AITUNNEL_MODEL_CANDIDATES`. Модели содержащие `flux` или `sdxl` автоматически исключаются (не поддерживаются AITunnel).

**Fallback модели:**
При 400 и тексте «model not found» используется следующая модель из списка кандидатов. Если все модели недоступны — ошибка `503 Provider Model Unavailable`.

**Санитизация ENV:**
Все переменные окружения автоматически очищаются от пробелов и комментариев (всё после `#`).

**Мониторинг:**
Используйте эндпоинт `GET /api/ai/providers/aitunnel/health` для проверки конфигурации и выбранной модели:
```bash
# Обычный запрос
curl http://localhost:8000/api/ai/providers/aitunnel/health

# Сброс кэша модели
curl http://localhost:8000/api/ai/providers/aitunnel/health?refresh=1
```

#### Альтернативные провайдеры

Для настройки других провайдеров см. `app/services/image_style_adapter.py`:
- **Replicate**: установите `REPLICATE_API_TOKEN`
- **ComfyUI**: установите `COMFYUI_API_URL`
- **Stable Diffusion WebUI**: установите `SD_WEBUI_API_URL`

---

**Создано с ❤️ для автоматизации контента**






























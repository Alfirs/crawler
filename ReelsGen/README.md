# 🎨 Instagram Carousel Generator

Минимальный MVP для создания профессиональных каруселей Instagram с использованием NeuroAPI.

## ✨ Возможности

- 🤖 **Автоматическая генерация контента** - ИИ создаёт план карусели и тексты
- 🎨 **Уникальные изображения** - каждый слайд получает стильное ИИ-изображение
- 📝 **Красивая типографика** - профессиональная вёрстка с шрифтом Inter
- 💧 **Водяные знаки** - поддержка текстовых и PNG водяных знаков
- 📱 **Instagram-ready** - готовый формат 1024×1024 пикселей
- 💰 **Экономично** - использует дешёвые модели NeuroAPI

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка конфигурации

Скопируйте `env.example` в `.env` и заполните данные NeuroAPI:

```bash
# Windows
copy env.example .env

# Linux/macOS  
cp env.example .env
```

Отредактируйте `.env`:
```
NEUROAPI_BASE_URL=https://neuroapi.host
NEUROAPI_API_KEY=sk-ваш-ключ-здесь
NEUROAPI_TEXT_MODEL=qwen2.5-mini-instruct
NEUROAPI_IMAGE_MODEL=flux-schnell
NEUROAPI_DRYRUN=false
```

### 3. Установка шрифтов (опционально)

Для лучшего качества скачайте шрифты Inter:
1. Перейдите на https://fonts.google.com/specimen/Inter
2. Скачайте файлы `Inter-Regular.ttf` и `Inter-Bold.ttf`
3. Поместите их в `app/assets/fonts/`

### 4. Запуск приложения

```bash
uvicorn app.min_app:app --host 127.0.0.1 --port 8010 --reload
```

Откройте в браузере: http://127.0.0.1:8010

## 📖 Использование

1. **Загрузите изображение** для обложки
2. **Введите заголовок** карусели  
3. **Выберите количество слайдов** (2-10)
4. **Добавьте водяной знак** (опционально)
5. **Нажмите "Сгенерировать"**
6. **Скачайте ZIP** с готовыми слайдами

## 🏗️ Структура проекта

```
ReelsGen/
├── app/
│   ├── min_app.py              # FastAPI приложение
│   ├── services/
│   │   ├── neuroapi.py         # Интеграция с NeuroAPI
│   │   ├── layout.py           # Работа с текстом и вёрсткой
│   │   ├── render.py           # Рендеринг слайдов
│   │   └── watermark.py        # Водяные знаки
│   ├── templates/
│   │   └── ui.html             # Веб-интерфейс
│   ├── static/
│   │   └── styles.css          # Стили
│   └── assets/
│       └── fonts/              # Шрифты Inter
├── requirements.txt
├── env.example
└── README.md
```

## ⚙️ Конфигурация

### Переменные окружения

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `NEUROAPI_BASE_URL` | URL NeuroAPI | `https://neuroapi.host` |
| `NEUROAPI_API_KEY` | Ваш API ключ | - |
| `NEUROAPI_TEXT_MODEL` | Модель для текста | `qwen2.5-mini-instruct` |
| `NEUROAPI_IMAGE_MODEL` | Модель для изображений | `flux-schnell` |
| `NEUROAPI_DRYRUN` | Режим тестирования | `false` |

### Режим разработки

Для тестирования без API вызовов установите `NEUROAPI_DRYRUN=true`. 
В этом режиме будут использоваться заглушки вместо реальных запросов к NeuroAPI.

## 🛠️ Технические детали

### Архитектура

- **FastAPI** - веб-фреймворк
- **Pillow** - обработка изображений  
- **httpx** - HTTP клиент для NeuroAPI
- **Jinja2** - шаблонизатор

### Алгоритм работы

1. **Планирование** - ИИ генерирует JSON план с заголовками и тезисами
2. **Обложка** - накладывает заголовок на загруженное изображение
3. **Контент** - для каждого слайда генерирует фон и размещает текст
4. **Сборка** - упаковывает все слайды в ZIP архив

### Оптимизация затрат

- Один запрос к LLM для всего плана карусели
- Использование дешёвых моделей NeuroAPI
- Размер изображений 1024×1024 (баланс качества/цены)
- Локальная обработка текста без дополнительных API вызовов

## 🔧 Устранение неисправностей

### Частые проблемы

**Ошибка импорта модулей:**
```bash
# Запускайте из корневой директории проекта
cd ReelsGen
uvicorn app.min_app:app --reload
```

**Не работает NeuroAPI:**
- Проверьте API ключ в `.env`
- Убедитесь что модели доступны в NeuroAPI
- Попробуйте режим `NEUROAPI_DRYRUN=true` для тестирования

**Проблемы со шрифтами:**
- Приложение работает без шрифтов Inter
- Для лучшего качества установите их в `app/assets/fonts/`

**Медленная генерация:**
- Генерация может занимать 30-60 секунд
- Это нормально для обработки нескольких ИИ запросов

### Логи и отладка

Запуск с подробными логами:
```bash
uvicorn app.min_app:app --host 127.0.0.1 --port 8010 --reload --log-level debug
```

## 📝 Примеры использования

### Бизнес-карусель
- Заголовок: "5 ошибок в email-маркетинге"
- Слайдов: 6
- Результат: обложка + 5 слайдов с ошибками и советами

### Образовательный контент
- Заголовок: "Как изучать Python эффективно"
- Слайдов: 8  
- Результат: пошаговый план изучения

### Личный бренд
- Заголовок: "Мой путь в дизайне"
- Слайдов: 5
- Результат: история и достижения

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи приложения
2. Убедитесь в корректности конфигурации NeuroAPI
3. Попробуйте режим `DRYRUN=true` для исключения проблем с API

## 📄 Лицензия

Этот проект создан как MVP для демонстрации возможностей NeuroAPI в генерации контента для социальных сетей.

---

💡 **Совет**: Используйте яркие, контрастные изображения для обложки. ИИ автоматически создаст стильные минималистичные фоны для остальных слайдов в едином стиле.


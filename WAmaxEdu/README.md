# WAmaxEdu Integration Platform

## 📌 Описание проекта
**WAmaxEdu** — это интеграционный шлюз (Middleware), связывающий мессенджеры (**WhatsApp**, **MAX Messenger**) с CRM-системой **Bitrix24**. Решение позволяет операторам Bitrix24 общаться с клиентами в WhatsApp и MAX через стандартный интерфейс чатов, используя единое окно.

## 🛠 Технологический стек

### Core
- **Runtime:** Node.js (v18+)
- **Language:** TypeScript
- **Framework:** Express.js (легковесный REST API)
- **Process Manager:** `tsx` для разработки, стандартный `node` для продакшена.

### WhatsApp Gateway
- **Engine:** [Evolution API](https://github.com/EvolutionAPI/evolution-api) v2 (на базе Baileys).
- **Protocol:** WebSocket (подключение к WhatsApp без официального API).
- **Database:** PostgreSQL (для хранения сессий Evolution API).
- **Cache:** Redis (для кэширования состояний Evolution API).
- **Containerization:** Docker & Docker Compose.

### Integrations
- **Bitrix24:** REST API (Incoming Webhooks + Polling).
- **MAX Messenger:** Webhook JSON API.

---

## 🏗 Архитектура и Логика

Система состоит из двух основных сервисов:

1. **Evolution API (Docker):**
   - Отвечает за поддержку соединения с WhatsApp.
   - Генерирует QR-коды для авторизации.
   - Принимает сообщения из WhatsApp и отправляет их через Webhook в основной сервис.
   - Предоставляет API для отправки сообщений в WhatsApp.

2. **Integrations Service (Node.js):**
   - **Входящий поток (WhatsApp/MAX -> Bitrix24):**
     - Принимает вебхуки от Evolution API и MAX.
     - Создает чаты в Bitrix24 (`im.chat.add`).
     - Форматирует и отправляет сообщения (`im.message.add`) с пометкой `SYSTEM: 'Y'`, чтобы визуально выделить сообщения клиентов.
   - **Исходящий поток (Bitrix24 -> WhatsApp/MAX):**
     - Использует **Long Polling** (`Bitrix24PollerService`) для опроса новых сообщений в чатах Bitrix24.
     - Фильтрует сообщения операторов.
     - Маршрутизирует ответы в соответствующий канал (WhatsApp или MAX) через провайдеры.

---

## 🔥 Ключевые фишки ("Hack & Tricks")

1. **Webhook-only Bitrix24 Integration:**
   - Мы обошли ограничение Bitrix24 на использование официальных коннекторов (которые требуют OAuth и HTTPS) путем эмуляции чатов через REST API.
   - **Polling вместо Outgoing Webhooks:** Поскольку бесплатные/базовые тарифы Bitrix24 ограничивают исходящие вебхуки, мы реализовали умный поллинг, который проверяет только активные чаты, минимизируя нагрузку на API.

2. **Визуализация сообщений:**
   - Входящие сообщения от клиентов помечаются как "Системные" (`SYSTEM: 'Y'`) в Bitrix24. Это позволяет оператору визуально отличать сообщения клиента от своих собственных, даже если технически они отправлены от имени одного API-пользователя.

3. **Multi-Provider Routing:**
   - Единый контроллер автоматически определяет источник сообщения и выбирает нужный драйвер (`WhatsAppProvider` или `MaxProvider`), позволяя масштабировать систему на новые мессенджеры.

4. **Self-Healing Docker Stack:**
   - Evolution API автоматически перезапускается и восстанавливает сессию WhatsApp при разрывах соединения благодаря настройкам Docker Compose и Redis.

---

## 🚀 Установка и Запуск

### Предварительные требования
- Node.js v18+
- Docker & Docker Compose
- Аккаунт Bitrix24 (права на создание входящего вебхука)

### 1. Настройка окружения
Создайте файл `.env` в корне проекта:
```env
# Server
PORT=3000

# Bitrix24
BITRIX24_WEBHOOK_URL=https://your-domain.bitrix24.ru/rest/1/xxxxxxxx/
BITRIX24_TEST_LINE_ID=1

# MAX Messenger
MAX_API_URL=https://api.max.com
MAX_BOT_TOKEN=your_token

# Evolution API
EVOLUTION_API_URL=http://localhost:8080
EVOLUTION_API_KEY=your_secret_key
EVOLUTION_INSTANCE_NAME=wamaxedu
```

### 2. Запуск Evolution API
```bash
docker compose -f docker-compose.evolution.yaml up -d
```

### 3. Запуск сервиса интеграций
```bash
npm install
npm run start:integrations
```

### 4. Подключение WhatsApp
1. Откройте Evolution Manager (обычно `http://localhost:8080` или используйте API).
2. Сканируйте QR-код в приложении WhatsApp.

---

## 📁 Структура проекта
```
src/
├── controllers/          # Обработчики вебхуков (Bitrix, MAX, Integrations)
├── services/
│   ├── providers/        # Логика работы с API (WhatsApp, Bitrix24, MAX)
│   ├── inbound-webhook.service.ts  # Роутинг входящих сообщений
│   └── bitrix24-poller.service.ts  # Сервис опроса Bitrix24
├── config/               # Конфигурация и типизация ENV
└── main.ts               # Точка входа
```
